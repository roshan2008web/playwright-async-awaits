{
  "name": "playwright-async-labs",
  "version": "1.0.0",
  "private": true,
  "description": "Small labs to learn async/await and Promise.all with Playwright",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2"
  }
}
